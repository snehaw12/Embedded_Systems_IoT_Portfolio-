#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x;
    int n,i,a;
    scanf("%d",&n);
    for(i=0;i<n;i++){
    scanf("%d",&a);
    x=(x*10)+a;


    }

    printf("%d",x);
    return 0;
}

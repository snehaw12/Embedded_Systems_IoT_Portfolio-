#include <stdio.h>
#include <stdlib.h>

int main()
 {
    int l,b,a;
    scanf("%d",&l);
    scanf("%d",&b);
    a=l*b;
    printf("Length of the rectangle %d" , l);
    printf("breadth of the rectangle %d", b);
    return 0;
}

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,c,d,e,P;
    scanf("%d", & a);
    scanf("%d", & b);
    scanf("%d", & c);
    scanf("%d", & d);
    scanf("%d", & e);
    P = (a+b+c+d+e)%
    printf("percentage of the five subject %d", P);
    return 0;
}

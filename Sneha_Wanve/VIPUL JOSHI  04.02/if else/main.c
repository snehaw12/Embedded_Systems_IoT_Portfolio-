#include <stdio.h>
#include <stdlib.h>

int main()
{
    int  a,b;
    scanf("&d",&a);

    if (a > 0)
    {
     printf("a is positive");
    }

    else
    {
        printf("a is negitive");
    }

    return 0;
}

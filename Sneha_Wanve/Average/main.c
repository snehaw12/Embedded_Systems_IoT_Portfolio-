#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,c,d,e,A;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    scanf("%d", &d);
    scanf("%d", &e);
    A=a+b+c+d+e;
    printf("Average of the five subjects %d" ,A);
    return 0;
}
